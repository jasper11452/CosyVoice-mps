# CosyVoice MPS-Only Requirements
# Core dependencies for Apple Silicon (MPS) inference

# PyTorch (install with pip install torch torchaudio)
torch>=2.0.0
torchaudio>=2.0.0

# Core ML/NLP
transformers>=4.30.0
tiktoken>=0.5.0

# Model loading
modelscope>=1.9.0

# Configuration
hyperpyyaml>=1.0.0
omegaconf>=2.3.0

# Audio processing
openai-whisper>=20230918
scipy>=1.10.0

# ONNX Runtime (CPU only for MPS systems)
onnxruntime>=1.15.0

# Tensor operations
einops>=0.6.0

# Utilities
numpy>=1.22.0
tqdm>=4.65.0
inflect>=6.0.0
regex>=2023.0.0

# Optional: Web UI
gradio>=4.0.0

# Text processing
wetext

# Note: Matcha-TTS is included as a submodule in third_party/
# Install with: pip install -e third_party/Matcha-TTS
